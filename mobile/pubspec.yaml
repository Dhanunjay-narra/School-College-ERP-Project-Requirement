name: school_college_erp_mobile
description: Enterprise Unified Mobile Application for Students, Parents, and Faculty
version: 1.0.0+1
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_bloc: ^8.1.3
  google_fonts: ^6.1.0
  shared_preferences: ^2.2.2
  qr_code_scanner: ^1.0.1
  flutter_local_notifications: ^17.0.0
  geolocator: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
